import React from 'react'
import { View, Text } from 'react-native';

export default function Forgot({ navigation }) {
  return (
    <View>
      <Text>Get a verification code by SMS to:</Text>
    </View>
  );
}